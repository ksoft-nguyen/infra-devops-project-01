# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
.terraform.lock.hcl
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Secrets — never commit real credentials
secrets.tfvars
*.auto.tfvars
!*.tfvars.example
